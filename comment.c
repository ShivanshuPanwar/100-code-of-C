/* Write comments for programs a & b. */
#include<stdio.h>
int main()
{
    //  int side;
    // printf("enter side:",side);
    // scanf("%d",&side);
    // printf("area of square:%d",side*side); 
    return 0;
}